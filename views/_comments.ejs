<li id="comment-<%=comment._id%>">
    <p><%=comment.content%> : <%=comment.user.name%>
    <% if (locals.user && locals.user.id == comment.user.id){ %>
        <a class="delete-comment-button" href="comment/destroy/<%=comment._id%>"> <span><i class="fas fa-window-close"></i></span></a>
    <% } %>
    </p>
</li>